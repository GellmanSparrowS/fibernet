"""APP compatibility imports for the shared physical surrogate workflow."""
from fibernet.ml.physical_learning import (
    FEATURE_SCHEMA, FEATURE_NAMES, MODEL_SPECS, ACQUISITIONS,
    MODEL_PLUGINS, ACQUISITION_PLUGINS, register_model,
    register_acquisition, select_candidate, Regressor,
)
